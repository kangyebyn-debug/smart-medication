# Smart Medication — 프로젝트 가이드

## 언어 설정
- **모든 응답은 한국어로 작성합니다.** 기술 용어는 영어 그대로 사용 (API, React, FastAPI 등).
- 코드 주석은 꼭 필요한 경우에만 간결히.

---

## 프로젝트 개요
- **리포 이름**: smart-medication
- **앱 표시명**: 스마트 메디케이션 (또는 "똑똑한 복약")
- **리포지터리**: https://github.com/kangyebyn-debug/smart-medication
- **로컬 경로**: `D:\Project_KYB\smart-medication`
- **라이선스**: MIT
- **목표**: 약물-약물 + 약물-음식 상호작용을 **일반인이 쉽게** 확인할 수 있는 반응형 웹앱
- **포트폴리오 성격**: Python / React 학습 겸 실전 프로젝트

---

## 사용자 배경
- 의료정보과 졸업 → 의약품 도메인 지식 보유
- SQL 경험: Oracle, PostgreSQL, MariaDB, MSSQL(SSMS)
- 프론트: Vue3 경험 있음, **React는 이 프로젝트에서 새로 학습**
- Python 초심자 (3.11.7 설치됨), **FastAPI 처음**
- Windows 11 환경
- 회사 기본은 CMD/PowerShell, **이 프로젝트만 Git Bash** 연습용

---

## 참조 프로젝트
- `d:\deepddi2`: KAIST의 DDI 예측 연구 코드. **이 프로젝트에선 AI 모델로 직접 쓰지 않음**. 참조/학습용. 실제 데이터는 한국 공공 API 사용.

---

## 기술 스택 (확정)

| 영역 | 선택 | 비고 |
|---|---|---|
| 백엔드 | Python 3.11.7 + FastAPI | 자동 문서 생성 + 타입 힌트 학습 |
| DB | PostgreSQL | Neon 무료 호스팅 예정 |
| 프론트엔드 | React | 학습 목적 |
| 저장소 | GitHub | Public, MIT License |
| 배포 (프론트) | Vercel 또는 GitHub Pages | 미정 |
| 배포 (백엔드) | Render 무료 티어 | 예정 |
| CI/CD | GitHub Actions | 예정 |
| 터미널 | Git Bash (이 프로젝트만) | `.vscode/settings.json` 설정됨 |

---

## 승인받은 데이터 API (5개, 공공데이터포털)

| # | API 이름 | 용도 | 만료일 |
|---|---|---|---|
| 1 | 한국의약품안전관리원_병용금기약물 | 약-약 상호작용 핵심 매핑 | 미명시 |
| 2 | 식품의약품안전처_의약품안전사용서비스(DUR)품목정보 | 상표명↔성분 변환, 자동완성 | 2028-04-22 |
| 3 | 식품의약품안전처_의약품안전서비스(DUR)성분정보 | 성분 상세 | 2028-04-22 |
| 4 | 식품의약품안전처_의약품개요정보(e약은요) | 일반의약품 주의사항 | 2028-04-22 |
| 5 | 식품의약품안전처_의약품 제품 허가정보 | 전문의약품 포함, 주의사항 전문 | 2028-04-22 |

**API 키는 재발급 완료.** `.env` 파일로 관리 예정 (절대 커밋 금지).

---

## 데이터 출처 원칙 (중요)

**우리 앱의 데이터는 오직 위 5개 공공 API(와 추후 추가 신청할 공공 API)에서만 가져옵니다.** 어떤 민간 상용 사이트의 데이터도 DB에 복사/저장하지 않습니다.

### druginfo.co.kr 취급 방침
- ❌ 스크래핑 금지, 데이터 DB 복사 금지
- ✅ **UX/UI 참고**용으로만 사용
- ✅ **샘플 테스트 케이스**로 약 조합 예시를 가져오는 것은 허용 (예: "A와 B가 병용금기라고 알려져 있는데, 우리 공공 API도 동일하게 응답하는지" 검증)
  - 이때 "정답"은 공공 API의 응답에서 나옵니다. druginfo에서 가져온 게 아닙니다.
  - 이건 계산기 만든 뒤 "2+2=4" 같은 알려진 답을 입력해 검증하는 것과 같은 개념

### 테스트 케이스에 대한 일반 원칙
- 검증용 약 조합은 **공공 출처(공식 허가사항, KIDS 공지 등) 또는 교과서적으로 유명한 사례**에서 선택하는 것이 가장 안전
- 어느 출처를 쓰든 **"이 조합이 어디서 왔는지" 코드 주석/테스트 설명에 명시**

---

## 데이터 아키텍처

핵심 원칙: **사용자는 상표명으로 검색하지만, 상호작용은 성분 단위로 매핑**

```
[사용자 입력: 상표명 "탁센레이디"]
    ↓
[DUR 품목정보 API] → 성분 추출 (예: 나프록센)
    ↓
[병용금기 API] → 성분 기준 상호작용 조회
    ↓
[DUR 품목정보 API] → 성분 → 다른 상표명들 역변환
    ↓
[사용자 표시: "탁센레이디(나프록센) ↔ 케라신주(XXX)는 병용금기"]
```

**DB 스키마 기본안:**
- `drugs` — 상표명, 품목코드
- `ingredients` — 성분명 (일반명)
- `drug_ingredients` — 다대다 (복합제 지원)
- `interactions` — 성분 A ↔ 성분 B, 위험도, 설명

---

## 단계별 계획

### 1단계 (현재): MVP — 약-약 상호작용만
- 승인된 5개 API로 약-약 검색/경고 기능 완성
- 상표명 검색 + 성분 정규화 + 병용금기 표시

### 2단계: 약-음식 상호작용 확장
- 식품영양성분 API 추가 신청
- 핵심 음식 30개 매핑 테이블 수기 구축 (커피, 우유, 자몽, 시금치 등)
- 주의사항 텍스트에서 음식 키워드 파싱

### 3단계: 고도화
- 특정연령대금기, 임부금기 등 DUR 추가 API
- NLP 기반 자동 확장

### 4단계 (장기 로드맵): DeepDDI2 통합
- **공공 API는 주(主) 엔진, DeepDDI2는 보조 엔진**으로 결합하는 2단 구조
- 시나리오: 공공 API에 없는 약 조합에 대해 DeepDDI2 예측으로 커버리지 확장
- 필요 작업: TensorFlow 2.x 마이그레이션, SMILES→성분명 변환 레이어, 예측 결과 신뢰도 표시
- **전제 조건**: 1~3단계 완성 후 시작. MVP 완성이 먼저.
- DeepDDI2 참조 경로: `d:\deepddi2` (별도 리포)

---

## 진행 상황 (2026-04-22 기준)

**완료:**
- ✅ 프로젝트 배경 / 기술 스택 확정
- ✅ API 5개 승인, 키 재발급 완료
- ✅ GitHub 리포 생성 (Public, MIT License)
- ✅ 로컬 폴더 클론 완료 (`D:\Project_KYB\smart-medication`)
- ✅ `.vscode/settings.json` (Git Bash 기본 터미널) 설정
- ✅ `CLAUDE.md` (이 파일) 작성

**다음 작업:**
- ⏳ 프로젝트 폴더 구조 (backend/frontend 분리)
- ⏳ Python 가상환경 세팅, FastAPI 설치
- ⏳ `.env` + `.gitignore` 확장
- ⏳ 첫 API 테스트 스크립트 — 알려진 병용금기 조합을 **입력**으로 넣어 공공 API 호출 파이프라인이 제대로 동작하는지 검증 (후보 조합: 탁센레이디+케라신주, 또는 와파린+아스피린 등). 답은 공공 API 응답으로 판단. 민간 사이트 데이터 복사 아님. 상세 원칙은 위 "데이터 출처 원칙" 섹션 참조.
- ⏳ DB 스키마 설계
- ⏳ 프론트(React) 뼈대
- ⏳ 배포 (Render, Vercel)

---

## 작업 스타일 / 협업 원칙

1. 새로운 도구/라이브러리 도입 시 **왜 선택했는지** 한 줄로 설명
2. 큰 변경 전에는 계획 공유 후 **승인 받기**
3. 복잡한 설정은 **단계별로 나누어** 설명
4. 한 번에 한두 스텝씩. 초보에게 정보 폭탄 금지
5. Python/React가 학습 목적임을 유념 — **코드가 돌아가는 것**보다 **이해하는 것**이 우선

---

## 보안 주의사항

- API 키, 비밀번호, `.env` 파일은 **절대 커밋 금지**
- `.gitignore`에 `.env` 등록 필수
- 공공데이터 API 키가 Git 히스토리에 들어가면 **즉시 재발급**
- GitHub 2FA 활성화 권장

---

## 참고 자료

- UX 참고 사이트 — **크롤링 금지, DB 복사 금지**. UX 영감 및 테스트 케이스 출처로만 (상세: 위 "데이터 출처 원칙" 섹션): https://www.druginfo.co.kr/p/contra-product-search/
- 공공데이터포털: https://www.data.go.kr
- 식의약 데이터포털: https://data.mfds.go.kr
- 의약품안전나라: https://nedrug.mfds.go.kr
